@AGENTS.md

# 応答言語

このプロジェクトでは常に日本語で応答すること。

# コミット・プッシュ方針

機能の実装や修正を行い、動作確認(lint・ビルド・型チェック・可能であれば実際の動作確認)が完了したら、
ユーザーからの指示を待たずに自動的にGitへコミットし、GitHub(origin)へプッシュすること。
「コミットしていいですか」「プッシュしていいですか」と都度確認する必要はない。

ただし、以下の安全なGit運用ルールは引き続き遵守すること:
- force push・履歴の書き換え(rebase -i、amendでの上書き等)は行わない
- コミット前に差分・ステージ内容を確認し、シークレットや認証情報を含むファイルを誤ってコミットしない
- コミットメッセージは変更内容が分かるように簡潔に書く
- このリポジトリはmainブランチに直接コミット・プッシュする運用なので、特別な指示がない限りmainへプッシュする
- データベースのマイグレーションファイルなど、リポジトリへの反映と本番DBへの適用は別工程であることに注意する(コミット・プッシュは自動で行うが、Supabase側へのマイグレーション適用は別途ユーザーに依頼する)
